# M0 Acceptance Record — Deterministic Kernel (§24.3)

| Field | Value |
| --- | --- |
| Spec | `HIBIKI_MVP_SPEC_v0.2.1.md` §24.1 / §24.3 |
| Date | 2026-09-07 (UTC+8); status revised 2026-09-11 |
| Verdict | **FAIL — freeze not authorized** |
| Prior freeze claim | `94ddf91` PASS withdrawn |
| Evidence root | [`evidence/m0-2026-09-07/`](evidence/m0-2026-09-07/) |

## 1. Status of prior freeze claim

The earlier conclusion that **M0 control invariants PASS and schema may freeze** at `94ddf91` is **withdrawn**.

Until a new measured freeze object records PASS on all counterexamples (plus H-001–H-021 / H-044–H-050), **do not freeze schema or enter M1**.

## 2. Historical suite facts (kept)

| Measured tree | Suite | Note |
| --- | --- | --- |
| Prior repair waves | 55 / 62 / 66 passed | Factual counts only; not stage-gate PASS |

## 3. Round-4 blockers (against `b390881` and follow-ups)

| ID | Severity | Finding |
| --- | --- | --- |
| R4-1 | P0 | IN_FLIGHT `agent.start` + Gate freed Workspace → double writer (INV-07) |
| R4-2 | P1 | `ArtifactRow.verdict` masked later VERIFY FAIL for same hash |
| R4-3 | P1 | Same Result dual refs (`artifact_refs` + `acceptance_evidence`) could IntegrityError |
| R4-4 | P1 | USER_AGENT could forge `submit_result` |
| R4-5 | P1 | Plan-removed Work Unit left RUNNING and blocked final accept |
| R4-6 | P1 | Worker-bound Gate left WU BLOCKED with stale `active_run_id` |
| R4-7 | P1 | Contract Delta approve closed Gate without applying (no APPROVED_PENDING_APPLY) |

Earlier rounds R2 / R3 findings remain documented in git history; code addresses them plus R4.

## 4. Stage gate conclusion

**M0 control invariants: FAIL (freeze not authorized).**

Do not treat green pytest alone as freeze permission. Publish a new §24.3 record with freeze commit SHA after formal re-acceptance.
