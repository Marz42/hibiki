# M0 Acceptance Record — Deterministic Kernel (§24.3)

| Field | Value |
| --- | --- |
| Spec | `HIBIKI_MVP_SPEC_v0.2.1.md` §24.1 / §24.3 |
| Date | 2026-09-07 (UTC+8) |
| Verdict | **PASS** |
| Freeze commit | `94ddf91cad5cf3c355963e7c0939b59cd3d45167` |
| Evidence root | [`evidence/m0-2026-09-07/`](evidence/m0-2026-09-07/) |

## 1. Environment (§24.1)

| Item | Recorded value |
| --- | --- |
| Hardware | Intel Core i5-14400F, 16 logical CPUs, ~32 GiB RAM, local SSD |
| OS | Windows 11 (`10.0.26200`) |
| Python | 3.12.13 (`.venv` via `uv`) |
| SQLite | 3.50.4 (CPython bundled) |
| Dependencies | `uv.lock` SHA256 `4A59AC044FA3B5D327D1DD964012AE452B9822CF8E1F8EB5C86D31C244BC02A4` |
| Stack | SQLAlchemy 2.0.52, Alembic 1.19.2, Pydantic 2.13.5, hibiki 0.1.0 |
| Adapters | FakeAgentAdapter / FakeClock / FakeExternalAdapter only |
| Model / Profile | none / in-test `fake` profile |
| Network | No LLM; suite does not require public network |
| DB / fixtures | Ephemeral per-test `tmp_path` SQLite; in-repo tests |
| Git freeze object | `94ddf91cad5cf3c355963e7c0939b59cd3d45167` |
| Parent | `2e3b0b18e044ab0c11d162f44f89b2aaeb6eabe8` |
| Working tree at freeze | clean at `94ddf91` (code + evidence co-committed) |
| Source tree SHA256 | see `evidence/m0-2026-09-07/source-tree.sha256` |

Full dump: [`evidence/m0-2026-09-07/environment.txt`](evidence/m0-2026-09-07/environment.txt).

### Schema freeze pointer

M0 measured tree is git object **`94ddf91cad5cf3c355963e7c0939b59cd3d45167`**. Do not treat parent `2e3b0b1` alone as M0-passed. A follow-up docs-only commit may refresh this pointer text; the freeze object for implementation sources remains `94ddf91`.

## 2. Command and aggregate result

```bash
uv run pytest tests/ -vv --tb=short \
  --junitxml=docs/acceptance/evidence/m0-2026-09-07/junit.xml
```

| Metric | Value |
| --- | --- |
| Collected | 55 |
| Passed | 55 |
| Failed / errors / skipped | 0 / 0 / 0 |
| Duration | 17.24 s |
| Log | [`pytest.log`](evidence/m0-2026-09-07/pytest.log) |
| JUnit | [`junit.xml`](evidence/m0-2026-09-07/junit.xml) |
| Case summary | [`junit-summary.txt`](evidence/m0-2026-09-07/junit-summary.txt) |

## 3. §24.3 mandatory gates

| Gate | Evidence | Result |
| --- | --- | --- |
| H-001–H-021 + H-044–H-050 (28 Fake scenarios) | §4 table; `tests/application/test_m0_acceptance.py` | PASS |
| 100 fixed seeds × ≥200 legal/illegal/dup/late inputs; no illegal transition / dup formal approval / dup valid dispatch | `test_100_fixed_seeds` (10.79 s) | PASS |
| Crash before commit + after commit before notify; no partial State/Event/Outbox; scan recovers | `test_h009_*` | PASS |
| Empty-dir migrate to current schema; unknown schema version refuses start with diagnostic | `test_migrate_from_empty_dir`, `test_refuse_start_without_schema` | PASS |
| Runnable without LLM / public network | Fake adapters only | PASS |

Supporting (not substitute for H-IDs): domain transitions, SQLite pragmas, restart reconcile, and 15 P0/P1 regression cases — all PASS in the same run.

## 4. Scenario results (28 required)

| ID | Scenario (SPEC §25) | Test | Result | Evidence |
| --- | --- | --- | --- | --- |
| H-001 | No ACTIVE Contract → no business Agent dispatch | `test_h001_no_active_contract_no_dispatch` | PASS | junit / pytest.log |
| H-002 | User Agent / Internal cannot approve as Human | `test_h002_user_agent_cannot_approve`, `test_h002_internal_cannot_approve` (+ P0 `approve_side_effect`) | PASS | junit / pytest.log |
| H-003 | Stale approval after Contract/Explanation change | `test_h003_stale_approval_rejected` | PASS | junit / pytest.log |
| H-004 | Duplicate Human Decision → one formal decision | `test_h004_duplicate_decision_idempotent` | PASS | junit / pytest.log |
| H-005 | Same idempotency key, different payload → conflict | `test_h005_idempotency_conflict` | PASS | junit / pytest.log |
| H-006 | Cancel before start Outbox send → no new Agent | `test_h006_cancel_invalidates_pending_start` | PASS | junit / pytest.log |
| H-007 | Gate freezes new dispatch | `test_h007_gate_freezes_dispatch` | PASS | junit / pytest.log |
| H-008 | Pause/Resume with pending Gate | `test_h008_pause_resume_with_pending_gate` | PASS | junit / pytest.log |
| H-009 | Crash around State/Event/Outbox transaction | `test_h009_crash_before_commit_no_partial`, `test_h009_crash_after_commit_scan_recovers` | PASS | junit / pytest.log |
| H-010 | Late result after timeout does not overwrite | `test_h010_late_result_does_not_overwrite` | PASS | junit / pytest.log |
| H-011 | Stale Planner generation rejected | `test_h011_stale_planner_generation` | PASS | junit / pytest.log |
| H-012 | Profile version frozen on Run | `test_h012_profile_version_frozen_on_run` | PASS | junit / pytest.log |
| H-013 | Verify FAIL blocks VERDICT_PASS dependency | `test_h013_verify_fail_blocks_verdict_pass_dep` | PASS | junit / pytest.log |
| H-014 | Plan changing auth/acceptance → Contract Delta | `test_h014_plan_authorization_change_needs_delta` | PASS | junit / pytest.log |
| H-015 | Unrelated Plan bump keeps valid result | `test_h015_unrelated_plan_bump_keeps_valid_result` | PASS | junit / pytest.log |
| H-016 | Upstream artifact hash change invalidates PASS | `test_h016_upstream_hash_change_invalidates_pass` | PASS | junit / pytest.log |
| H-017 | No COMPLETED without Final Acceptance | `test_h017_no_completed_without_acceptance` | PASS | junit / pytest.log |
| H-018 | Budget ceiling → resource Gate | `test_h018_budget_gate` | PASS | junit / pytest.log |
| H-019 | Expired lease + live Fake writer → quarantine | `test_h019_lease_expired_writer_alive_quarantine` | PASS | junit / pytest.log |
| H-020 | Restart keeps PAUSED / WAITING_HUMAN | `test_h020_reconcile_keeps_paused_waiting` | PASS | junit / pytest.log |
| H-021 | Core sequence monotonic despite producer clock | `test_h021_event_sequence_monotonic_ignores_producer_clock` | PASS | junit / pytest.log |
| H-044 | External action without preauth/approval | `test_h044_no_dispatch_without_approval` | PASS | junit / pytest.log |
| H-045 | Param change invalidates old approval | `test_h045_param_change_invalidates_old_approval` | PASS | junit / pytest.log |
| H-046 | Crash around external dispatch keeps identity | `test_h046_dispatch_crash_leaves_identity` | PASS | junit / pytest.log |
| H-047 | Idempotent reconcile → single external effect | `test_h047_idempotent_reconcile_single_effect` | PASS | junit / pytest.log |
| H-048 | Non-idempotent UNKNOWN → no auto redispatch | `test_h048_non_idempotent_unknown_no_auto_redispatch` | PASS | junit / pytest.log |
| H-049 | Same logical action reuses effect identity | `test_h049_same_logical_action_reuses_effect` | PASS | junit / pytest.log |
| H-050 | Cancel after dispatch keeps uncertainty | `test_h050_cancel_after_dispatch_keeps_uncertainty` | PASS | junit / pytest.log |

## 5. Property / recovery / regression (same run)

| Suite | Cases | Result |
| --- | --- | --- |
| `tests/property/test_random_schedule.py` | seeds `0..99`, 200 steps each | PASS |
| `tests/recovery/test_recovery.py` | migrate empty; refuse unknown schema; reconcile after restart | PASS |
| `tests/application/test_m0_review_regressions.py` | 15 P0/P1 counterexamples | PASS |
| Domain / SQLite unit | 6 | PASS |

Failed seeds: none (replay N/A).

## 6. Known limits (ADR-0002)

Recorded intentional M0 boundaries (not silent weakenings):

- No real model adapter, sandbox, or external provider
- ContextManifest recorded; materialization stub
- Workspace fencing uses in-memory Fake writers, not OS processes
- CLI injects AuthContext; no browser session / CSRF
- Single Core instance lock is SQLite/file lock on data directory

Source: [`docs/adr/0002-known-limits-m0.md`](../adr/0002-known-limits-m0.md).

## 7. Stage gate conclusion

Per §24.3: Fake H-001–H-021 / H-044–H-050 all passed; 100-seed trajectories clean; crash injection and migration/schema diagnostics passed; suite runs without LLM or public network.

**M0 control invariants for freeze commit `94ddf91`: PASS.**

Schema / first-version protocol may now be frozen against this object before M1.
