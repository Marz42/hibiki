# M0 Acceptance Record — Deterministic Kernel (§24.3)

| Field | Value |
| --- | --- |
| Spec | `HIBIKI_MVP_SPEC_v0.2.1.md` §24.1 / §24.3 |
| Date | 2026-09-07 (UTC+8); status revised 2026-09-08 |
| Verdict | **FAIL — freeze not authorized** |
| Prior freeze claim | `94ddf91` PASS withdrawn |
| Evidence root | [`evidence/m0-2026-09-07/`](evidence/m0-2026-09-07/) |

## 1. Status of prior freeze claim

The earlier conclusion that **M0 control invariants PASS and schema may freeze** at `94ddf91` is **withdrawn**.

Counterexamples reproduced against that tree (and remaining after the first P0/P1 repair round) show SPEC violations that block stage gate:

| ID | Severity | Finding |
| --- | --- | --- |
| R2-1 | P0 | External side-effect dispatch still allowed under `PAUSED` or other blocking Human Gates (§7.3, §14.2) |
| R2-2 | P1 | Final acceptance could complete with FAIL/empty criterion evidence and other pending Gates (§20.1, INV-11) |
| R2-3 | P1 | `submit_result` treated as executor exit; Workspace released without Adapter exit confirm (§8.2) |
| R2-4 | P1 | Process LOST left Work Unit `RUNNING` with `active_run_id=None` and no retry path (§19.1) |
| R2-5 | P1 | Inbox replay wrapped failure payloads as `CommandResult.ok=True` (§12.2) |
| R2-6 | P1 | Random-schedule “100×200 inputs” overstated; early terminal `break` and no-op choices under-counted real `execute()` calls (§24.3) |

Until a new measured freeze object records PASS on these counterexamples (plus H-001–H-021 / H-044–H-050), **do not freeze schema or enter M1**.

## 2. Historical suite fact (kept)

At the prior measured tree, `uv run pytest tests/ -q` reported **55 passed**. That remains a factual suite count for that object; it does **not** imply §24.3 stage-gate PASS after the counterexamples above.

## 3. Repair direction (working tree; not yet a freeze)

Implementation work after this revision targets R2-1…R2-6 and a third counterexample round:

| ID | Severity | Finding |
| --- | --- | --- |
| R3-1 | P1 | Client `extra_evidence` could invent PASS hashes / override FAIL |
| R3-2 | P1 | In-place Spec upgrade let old Runs complete new definitions; DONE revived |
| R3-3 | P1 | Gate-revoked `agent.start` left CREATED Run occupying the Work Unit |
| R3-4 | P1 | Pause/Cancel did not stop SUCCEEDED Runs that still owned a live writer |

Addressed in code by: Artifact registry + Core-resolved evidence; immutable Work Unit identity on definition change; revoke CREATED Run with occupancy release on start invalidation; stop selection by live Workspace owner / Adapter inspect.

Re-run §24.3 and publish a **new** acceptance record with commit SHA, junit, and seed input-count evidence before any freeze claim.

## 4. Stage gate conclusion

**M0 control invariants: FAIL (freeze not authorized).**

Previous text claiming PASS / freeze-ready at `94ddf91` must not be used as the M1 entry gate.
