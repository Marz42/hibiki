# M2 Acceptance Record — Planner / DAG / Integration (§24.5)

| Field | Value |
| --- | --- |
| Spec | `HIBIKI_MVP_SPEC_v0.2.1.md` §24.5, with §5–9 / §11 / §12.3 / §16–17 / §20 |
| Date | 2026-09-17 |
| Checklist | [`docs/M2-CHECKLIST.md`](../M2-CHECKLIST.md) |
| Evidence root | [`evidence/m2-2026-09-17/`](evidence/m2-2026-09-17/) and [`evidence/m2-harness-smoke/`](evidence/m2-harness-smoke/) |
| Verdict | **PENDING — adversarial review of `1eb2477` found blocking P1s; not frozen for M3** |

M0 remains accepted at [`M0-2026-09-13.md`](M0-2026-09-13.md). M1 remains accepted at [`M1-2026-09-13.md`](M1-2026-09-13.md). Schema generation is `m2` (migration `0008_m2_messages`).

## 1. Gate status

| # | §24.5 requirement | Status | Measurement |
| --- | --- | --- | --- |
| G1 | H-033–H-038; H-011 / H-013–H-016 under multi-Agent Runtime | **PARTIAL** | Unit suite covers cases; review found auth/generation and VERIFY semantics gaps |
| G2 | Fixed complex sample: ≥2 parallel + INTEGRATE + VERIFY + FAIL→REPAIR | **NOT MET** | Live harness skipped FAIL→REPAIR and downgraded `VERDICT_PASS`→`DONE` |
| G3 | Barrier-proven parallel; shared-workspace writers serial | **MET** (Fake) | `test_h035_barrier_parallel_and_shared_workspace_serial` |
| G4 | Fake complex ×20 | **PARTIAL** | Parametrized tests exist; harness smoke mis-labeled COMPLETED without VERIFY |
| G5 | Planner crash / stale generation | **NOT MET** | Generation bump did not revoke PLAN Run; no real planner consume/checkpoint loop |
| G6 | Three live complex tasks, ≥2/3 inside budget | **NOT MET as §24.5 evidence** | Prior “3/3” used weak `ok` (run count only); review reproduced zero-artifact PASS |
| Gate | Complex Task closes the loop | **PENDING** | Do not enter M3 until this record is PASS |

## 2. Adversarial review findings (`1eb2477`)

Blocking P1 (must fix before re-acceptance):

1. Worker can cross-principal `dispatch_ready_runs` / unbound `post_task_message`; stale PLAN credential + new generation still proposes; generation bump keeps old PLAN Run.
2. VERIFY `verdict=FAIL` rewritten to PASS with auto PASS acceptance evidence (`api_agent` result builder).
3. `sandbox_exit_unconfirmed` still reported STOPPED; Docker probe treats query failure as not-running.
4. Planner recovery unwired: no PLAN RunInput/manifest; Results do not become TaskMessages; FakePlanner does not consume/propose.
5. Per-node `objective` / `input_refs` discarded at activate.
6. M2 harness success criteria too weak; live downgrades quality edges; FAIL→REPAIR not executed.

P2: truncated `fs.read` drops ContextAppend; Linux seed missing parent dirs for nested paths.

## 3. Repair status

Code fixes for the P1/P2 list are on the working tree (see checklist progress log).
Regression coverage: `tests/m2/test_review_regressions.py`. Non-property suite:
**304 passed, 1 skipped**; Ruff clean. Acceptance stays **PENDING** until Fake G1–G5 and
live G6 are re-run against this Core/harness and a new evidence directory is filed.
Do not freeze M2 or start M3 on the prior “3/3” pack.

## 4. Known limits (carried)

- In-process Core until M3.
- Windows uses path-mode `WorkspacePaths` (no directory FDs); Unix retains dir_fd walk.
