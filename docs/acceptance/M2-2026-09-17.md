# M2 Acceptance Record — Planner / DAG / Integration (§24.5)

| Field | Value |
| --- | --- |
| Spec | `HIBIKI_MVP_SPEC_v0.2.1.md` §24.5, with §5–9 / §11 / §12.3 / §16–17 / §20 |
| Date | 2026-09-17 |
| Checklist | [`docs/M2-CHECKLIST.md`](../M2-CHECKLIST.md) |
| Evidence root | [`evidence/m2-2026-09-17/`](evidence/m2-2026-09-17/) and [`evidence/m2-harness-smoke/`](evidence/m2-harness-smoke/) |
| Verdict | **G1–G5 PASS (Fake); G6 live executed 0/3 PASS — environmental BLOCKED** |

M0 remains accepted at [`M0-2026-09-13.md`](M0-2026-09-13.md). M1 remains accepted at [`M1-2026-09-13.md`](M1-2026-09-13.md). Schema generation is now `m2` (migration `0008_m2_messages`).

## 1. Gate status

| # | §24.5 requirement | Status | Measurement |
| --- | --- | --- | --- |
| G1 | H-033–H-038; H-011 / H-013–H-016 under multi-Agent Runtime | **MET** | `tests/m2/test_h033_h038_acceptance.py` (29 tests) |
| G2 | Fixed complex sample: ≥2 parallel + INTEGRATE + VERIFY + FAIL→REPAIR | **MET** | Topology in tests + `docs/m2/tasks/`; harness smoke 3/3 |
| G3 | Barrier-proven parallel; shared-workspace writers serial | **MET** | `test_h035_barrier_parallel_and_shared_workspace_serial` |
| G4 | Fake complex ×20, out-of-order / duplicate, zero PASS-gate bypass | **MET** | `test_g4_complex_fake_scenario_x20` parametrized 0–19 |
| G5 | Planner kill around proposal / message consume; stale generation refused | **MET** | `test_h036_checkpoint_and_stale_generation` |
| G6 | Three live complex tasks, ≥2/3 inside budget | **NOT MET** | Live against `deepseek-flash`: 3/3 tasks dispatched; every EXECUTE Run ended `BLOCKED`/`FAIL` with `workspace_missing` and Docker Desktop unreachable (`dockerDesktopLinuxEngine` pipe missing). Records in `evidence/m2-2026-09-17/live-runs/` |
| Gate | Complex Task closes the loop; no multi-Planner / Worker chat | **MET** (Fake) | PLAN Run + Message Store only |

## 2. What was built

| Component | Location |
| --- | --- |
| Checklist | `docs/M2-CHECKLIST.md` |
| Task messages + schema `m2` | `alembic/versions/0008_m2_messages.py`, `TaskMessageRow` |
| PLAN Run + `ActivePlanRunMarker` | `dispatch_planner_run`, occupancy clear |
| Checkpoint + cursor (same txn) | `advance_planner_checkpoint` |
| Core-only spawn refusal | `request_spawn` + `spawn.refused` |
| Shared workspace + barrier Fake | optional `workspace_id` on plan nodes; `BarrierFakeAgentAdapter` |
| VERIFYING transitions | `_maybe_advance_verify_task_state` |
| Repair revision | `request_repair_plan` |
| Caps | `max_plan_revisions` / `max_work_units_per_task` enforced before supersede |
| H-033 refuse-without-destroy | Cross-task / DAG errors before ACTIVE supersede |
| Harness | `hibiki.interfaces.m2_runner`, `docs/m2/tasks/*.json` |

## 3. Evidence

| Measurement | Result |
| --- | --- |
| M2 acceptance suite | **29 passed** |
| Harness dry-run | **3/3** complex tasks completed (`evidence/m2-harness-smoke/summary.json`) |
| Live G6 | Executed 2026-09-17: model `deepseek-flash` via `https://api.deepseek.com`; **0/3** tasks `ok`; all worker Runs `BLOCKED` (workspace not materialized + Docker daemon unreachable). Collaboration/failure reconstructible from per-task JSON |

## 4. Known limits (carried + M2)

- In-process Core (M1 ADR); REST/MCP remains M3.
- Live G6 requires operator credentials and Docker sandbox (same as M1).
- Windows host may refuse `os.open` on some workspace directories under restricted temps; M1 acceptance was measured on Linux/WSL.

## 5. Freeze note

G1–G5 are satisfied on Fake multi-agent Runtime. **G6 is not met on this host**: the live model and Planner path ran, but EXECUTE Workers could not use fs/shell because workspaces were not seeded for M2 complex tasks and Docker Desktop was not running. Re-run G6 after (1) materializing/seeding per-WU workspaces in `m2_runner` and (2) starting Docker, then update this record before treating M2 as frozen for M3.